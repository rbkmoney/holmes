BW="\x1b[1;37m"
BR="\x1b[1;31m"
BY="\x1b[1;33m"
RS="\x1b[0m"

function err {
  echo -e "${BR}[ERROR]${RS}" "${1}"
  exit "${2:-127}"
}

function warn {
  echo -e "${BY}[WARNING]${RS}" "${1}"
}

function em {
  echo "${BW}${1}${RS}"
}
